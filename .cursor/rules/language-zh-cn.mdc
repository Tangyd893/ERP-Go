---
description: 与用户交流时统一使用简体中文，并规范英文术语写法
alwaysApply: true
---

# 语言：简体中文

- 面向用户的**所有回复、说明、总结**均使用**简体中文**。
- 内部推理与组织思路时，优先使用简体中文表述。

## 保持原文（代码相关）

与代码直接相关的内容**不翻译、不改写**，保持原文，例如：

- 源码、标识符、类型名、函数名、变量名
- 文件路径、目录名、包名、模块名
- 命令、CLI 参数、环境变量、配置键
- API 路径、HTTP 方法、JSON 字段名、数据库列名
- Git 分支名、commit hash、错误栈中的符号

## 英文术语（非代码名词）

对**没有通行中文译名**、或直译易歧义的业务/产品/技术名词，使用：

**`英文（中文释义）`**

示例：

- resume（简历）
- middleware（中间件）
- webhook（回调通知）
- worktree（Git 工作树）

规则：

- **首次出现**时给出「英文（中文释义）」；同一段落或同一话题内后续可只用英文或只用中文释义，以读起来自然为准。
- 已有稳定中文说法的常用词（如「数据库」「接口」「缓存」），直接用中文，不必套上述格式。
- 用户明确要求全英文的场景（如 commit message、PR 标题/描述）按用户要求处理，不受本条限制。
