# 项目里程碑与全流程待办清单

## 1. 文档目标

本文档把 ERP-Go 从动工到竣工的实施路径拆成可执行里程碑，明确每个阶段的核心功能模块、工程任务、验收标准和 Todo 清单。本文档用于后续排期、拆 issue、分配人员、跟踪进度和验收交付。

## 当前状态快照（2026-06-11）

- 后端检查：`go vet ./...`、`go build ./...` 通过；约 **214** 个 `Test*` 函数（含 finance/purchase 集成测、order repository testcontainers 3 测）。Windows 上 `order-service/.../repository` 包需 Docker，全量 `go test ./...` 会 FAIL；Linux CI 或跳过该包可绿。GOCACHE/GOMODCACHE 已固定到 `.cache/`。
- 测试覆盖：全部 13 个服务的 domain 包均有单元测试，P4 workflow 测试 13 个，Gateway 契约测试 4 个，`shared/outbox` 12 个，`testing/contract` 3 个，middleware 4 个，Order app 4 个。
- 服务结构：13 个服务均有 domain/app/HTTP；**12/13** 有 repository（`report-service` 为无仓储聚合）。P4–P7 后端闭环（波次/物流/面单、采购入库、财务结算）已在 commit `edcafbe` 合入。
- 网关路由：Gateway 已覆盖全部 13 个服务的代理路由，支持环境变量覆盖目标地址。
- 事件驱动：`shared/outbox` 实现 PG 存储、轮询、RabbitMQ 发布、Consumer/死信/Inbox；Order 进程内 Outbox 轮询 + P4 出库编排；采购入库 `HandleInboundReceived` 已接线。`shared/workflows` 含出库与入库编排及测试。
- 前端：`admin-web` 18 个视图、`warehouse-pda` 8 个视图（含 PackScan/WeighScan/看板）、`dashboard-web` ECharts 看板；PDA Gateway 联调已完成（见 [待办清单.md](./待办清单.md) §6.7）。
- SonarCloud（main，2026-06-11 CI 重扫 `b31368e`）：Quality Gate **未通过**（新代码安全 **E**、Hotspot 0%）；开放 Issue **37**；可靠性 **A**、新代码重复率 **2.97%** ✅。详见 [sonarcloud-report.md](./sonarcloud-report.md) 与 [待办清单.md](./待办清单.md) §3–§5。
- 迁移体系：服务级 migration 为唯一事实源（12/13，`report-service` 待补或标注无状态）；outbox 根级单独维护。
- 当前主线：T-412/T-002 exclusion + T-411 Hotspot → 剩余 smell → 可选 Playwright。详见 [待办清单.md](./待办清单.md)。

## 2. 总体阶段

| 阶段 | 名称 | 目标 | 交付状态 |
| --- | --- | --- | --- |
| P0 | 项目启动与工程底座 | 完成技术栈、目录、规范、基础设施和服务模板 | **已完成** |
| P1 | 账号租户与权限底座 | 完成登录、租户、组织、RBAC、审计 | **IAM App+HTTP 可用；Tenant App+HTTP 可用** |
| P2 | 商品与渠道基础 | 完成 SKU、平台 SKU 映射、店铺授权、订单导入 | **App+HTTP 可用；图片/CSV/Amazon 适配器未落地** |
| P3 | 订单与库存闭环 | 完成订单审核、库存余额、锁定、释放、扣减 | **App+HTTP 可用；Inventory DB 接入完成；集成测试待补** |
| P4 | WMS 出库与 TMS 发货 | 完成拣货、复核、打包、面单、出库、回传 | **Order 内 P4 编排+Outbox 轮询已有；PDA/Consumer/面单/回传待补** |
| P5 | 前端 MVP 与基础报表 | 完成管理后台、PDA、基础看板 | **admin 骨架完成；PDA 占位；Notification mock 待移除** |
| P6 | 采购与入库 | 完成供应商、采购、到货、质检、上架 | **workflow 编排代码已有；服务接线与业务闭环待通** |
| P7 | 财务结算与利润 | 完成应收应付、平台结算、成本、利润 | **App+HTTP+仓储已联通；核算逻辑待实现** |
| P8 | 测试加固与性能优化 | 完成自动化测试、压测、对账、异常补偿 | **180 个测试函数；集成/E2E/CI 仍不足** |
| P9 | 生产部署与竣工移交 | 完成部署、安全、备份、运维、培训、验收 | **网关 K8s yaml 已建立，生产配置未完成** |

## 3. P0 项目启动与工程底座

目标：建立可持续开发的工程底座，避免后续每个服务重复造轮子。

核心模块：

- 仓库目录。
- Go 服务模板。
- Vue 前端模板。
- Docker 中间件编排。
- 统一配置、日志、错误码、追踪、健康检查。
- 开发规范和提交规范。

Todo：

- [x] 确认顶层目录：`backend`、`frontend`、`docker`、`testing`、`docs`。
- [x] 创建 Go workspace 或多模块方案。
- [x] 创建 `backend/gateway` 服务骨架。
- [x] 创建 `backend/shared` 公共组件目录。
- [x] 创建服务模板，包含 Gin、配置加载、日志、错误响应、健康检查。
- [x] 创建 gRPC/proto 或 OpenAPI 契约目录。
- [x] 创建 Outbox/Inbox 公共接口设计。
- [x] 创建统一错误码规范和错误响应结构。
- [x] 创建统一日志字段：服务名、租户、用户、请求 ID、追踪 ID、业务单号。
- [x] 创建 Vue 3 + TypeScript + Vite 前端工作区。
- [x] 创建 `admin-web`、`warehouse-pda`、`dashboard-web`、`shared` 目录。
- [x] 创建 `docker/compose/docker-compose.dev.yml`。
- [x] 在 Compose 中加入 PostgreSQL、Redis、RabbitMQ、MinIO、OpenSearch。
- [x] 在 Compose 中加入 Prometheus、Grafana、Loki、Jaeger 或 Tempo。
- [x] 添加 `.env.example` 和本地开发配置模板。
- [x] 添加 Git 忽略规则，禁止提交本地密钥、中间件数据和日志。
- [x] 建立 `testing/contract`、`testing/integration`、`testing/e2e`、`testing/performance`。
- [x] 建立架构决策记录目录 `docs/adr/`。

验收标准：

- [x] 本地能启动所有中间件。
- [x] Go 服务模板能启动并返回健康检查。
- [x] Vue 前端模板能启动并访问。
- [x] 日志、错误码、追踪 ID、健康检查在模板中可复用。
- [x] 新服务可以基于模板在 30 分钟内创建骨架。

## 4. P1 账号租户与权限底座

目标：完成所有业务服务依赖的身份、租户、组织、权限和审计基础。

核心模块：

- API Gateway。
- IAM Service。
- Tenant Service。
- RBAC。
- 数据权限。
- 操作审计。

Todo：

- [x] 实现 API Gateway 路由、鉴权、请求 ID、追踪 ID 注入。
- [x] 实现登录、登出、刷新 Token。
- [x] 实现用户、角色、权限、菜单、按钮权限。
- [x] 实现租户、组织、部门、岗位 migration 和仓储。
- [x] 打通 Tenant App 层和 HTTP 接口。
- [ ] 实现仓库、店铺、供应商等数据权限模型。
- [x] 实现网关到 IAM 的权限校验接口。
- [x] 实现操作审计写入接口。
- [x] 实现默认超级管理员和初始化租户。
- [x] 实现前端登录页、主布局、菜单、权限路由。
- [x] 实现用户管理、角色管理、组织管理页面。
- [x] 编写 IAM 和 Tenant 的 migration。
- [ ] 编写 IAM 权限、认证、角色分配相关单元测试和接口测试。
- [ ] 编写 Tenant 组织、部门、岗位接口测试。

验收标准：

- [x] 管理员可以登录后台。
- [x] 不同角色看到不同菜单和按钮。
- [x] 业务请求必须带租户上下文。
- [x] 权限变更、登录、导出等操作写入审计。

## 5. P2 商品与渠道基础

目标：让平台订单能够被正确导入，并映射到内部 SKU。

核心模块：

- Product Service。
- Channel Service。
- File Service。
- 商品管理前端。
- 店铺授权前端。
- 平台订单导入。

Todo：

- [x] 实现 SPU、SKU、变体、组合商品模型。
- [x] 实现 SKU 条码、包装规格、重量尺寸、申报信息。
- [x] 实现平台 SKU、店铺 SKU、ASIN、FNSKU 映射。
- [x] 建立 File Service 领域模型和 migration。
- [~] 实现商品图片上传、下载和对象存储接入（File Service 领域就绪，MinIO 在 Compose 中，HTTP 上传待接线）。
- [x] 实现店铺、平台、站点、授权状态模型。
- [x] 实现平台 API 调用日志和同步任务模型。
- [x] 实现 CSV 订单导入作为首期兜底能力（ParseCSVOrders + ImportCSVOrders + POST /orders/import-csv）。
- [x] 设计 Amazon SP-API 适配器接口，首期可先使用 Mock（PlatformAdapter 接口 + MockAmazonAdapter + POST /orders/fetch-platform）。
- [x] 实现订单导入任务，按店铺 ID + 平台订单号幂等。
- [x] 实现商品列表、SKU 详情、SKU 映射页面。
- [x] 实现店铺授权、同步任务、导入结果页面。
- [x] 编写 Product、Channel、File migration。
- [x] 打通 Product、Channel、File 的 App 层和真实 HTTP 接口。
- [ ] 编写 SKU 映射、订单导入幂等测试。

验收标准：

- [x] 可以维护内部 SKU。
- [x] 可以建立平台 SKU 与内部 SKU 的映射。
- [x] 可以导入平台订单并生成订单导入事件（CSV 导入 + Amazon Mock 适配器）。
- [x] 重复导入同一平台订单不会重复创建（idempotency_key）。

## 6. P3 订单与库存闭环

目标：订单可以审核并锁定库存，库存账可追溯。

核心模块：

- Order Service。
- Inventory Service。
- 订单状态机。
- 库存余额。
- 库存锁定、释放、扣减。
- 库存流水。

Todo：

- [x] 实现销售订单、订单明细、订单地址、订单支付。
- [x] 实现订单状态机和状态历史。
- [ ] 实现订单导入应用服务。
- [x] 实现订单审核应用服务。
- [x] 实现订单异常处理。
- [x] 实现订单取消和库存释放流程。
- [x] 实现库存余额表。
- [x] 实现库存流水表。
- [x] 实现库存锁定表。
- [x] 实现库存锁定、释放、扣减接口。
- [x] 实现库存并发控制，使用事务 + 行锁或乐观锁。
- [x] 实现库存幂等键。
- [x] 将 Inventory HTTP handler 从内存模拟切换到数据库仓储。
- [x] 实现订单列表、订单详情、审核、异常处理页面。
- [x] 实现库存查询、库存流水页面。
- [x] 编写订单、库存 migration。
- [x] 编写订单状态机测试。
- [x] 编写库存并发锁定测试。
- [ ] 编写 Inventory 仓储层数据库集成测试。
- [ ] 编写订单审核到库存锁定集成测试。

验收标准：

- [ ] 订单可以从待审核流转到已审核。
- [ ] 审核通过后可以锁定库存。
- [ ] 库存不足时订单进入缺货或异常状态。
- [ ] 库存锁定、释放、扣减都有流水。
- [ ] 并发审核不会导致超卖。

## 7. P4 WMS 出库与 TMS 发货

目标：完成首期最关键的订单履约闭环。

核心模块：

- Warehouse Service。
- Transport Service。
- WMS PDA。
- 出库单。
- 拣货、复核、打包、称重。
- 物流规则。
- 面单和运单号。
- 发货回传。

Todo：

- [x] 实现仓库、库区、库位领域模型和表结构。
- [x] 实现出库单和出库明细领域模型和表结构。
- [x] 实现库存锁定成功后创建出库单。
- [x] 实现仓库已出库事件（outbound.shipped → inventory.deducted → order.shipped）。
- [x] 实现库存扣减消费逻辑（HandleOutboundShipped）。
- [x] 实现订单已发货状态更新。
- [x] 实现波次和拣货任务（Wave 领域/仓储/App/HTTP；PickTask 创建时自动生成）。
- [x] 实现 PDA 拣货扫码（PickScan 幂等+状态校验；PDA PickScan.vue）。
- [x] 实现 PDA 复核扫码（CheckScan SKU+数量校验；PDA CheckScan.vue）。
- [x] 实现打包、称重、包裹信息（PackageModel 持久化；Pack/Weigh 幂等；PDA PackScan/WeighScan.vue）。
- [x] 实现出库异常上报和处理（MarkOutboundAbnormal endpoint；状态机 OutboundAbnormal）。
- [x] 实现物流商、物流产品、物流规则领域模型和表结构。
- [x] 实现物流渠道匹配（MatchCarrier：按重量+国家+优先级匹配 ShippingRule）。
- [x] 实现面单创建接口，首期支持手工录入和 Mock 物流商（LabelAdapter 接口 + MockLabelAdapter + GenerateLabel HTTP）。
- [x] 实现发运单、包裹、轨迹基础模型。
- [x] 实现面单模型、面单文件和物流商适配器（LabelAdapter + LabelRequest/Response）。
- [x] 实现仓库已出库事件（Outbox → EventOutboundShipped）。
- [x] 实现库存扣减消费逻辑。
- [x] 实现订单已发货状态更新。
- [x] 实现渠道服务发货回传任务（Channel tracking_upload + Transport ChannelNotifyClient）。
- [x] 实现 WMS PDA 登录、任务列表、拣货、复核、打包页面（Login/PickScan/CheckScan/PackScan/WeighScan.vue）。
- [x] 实现物流配置、出库单、包裹和面单页面（WarehouseOutbound.vue + TransportManagement.vue）。
- [x] 编写 WMS、TMS migration。
- [x] 编写 PDA 重复扫码幂等测试（domain_test.go: TestOutboundStatusIdempotency）。
- [x] 编写出库到库存扣减集成测试（fulfillment_flow_test.go: TestP4HTTPFulfillmentChain）。
- [x] 编写发货回传契约测试（tracking_upload_contract_test.go: TestTrackingUploadContract）。

验收标准：

- [x] 订单审核后能生成出库单。
- [x] PDA 能完成拣货、复核、打包（页面+后端 API 齐备）。
- [x] 能生成或录入运单号（LabelAdapter + Mock 生成 trackingNo）。
- [x] 出库完成后库存扣减（Outbox → Inventory 事件链路）。
- [x] 订单状态更新为已发货（Outbox → Order 事件链路）。
- [x] 发货信息可以回传平台或进入重试队列（Channel tracking_upload + Transport ChannelNotifyClient）。

## 8. P5 前端 MVP 与基础报表

目标：让业务人员能够通过页面完成首期闭环操作，并看到基础报表。

核心模块：

- ERP 管理后台。
- WMS PDA。
- 经营看板。
- 基础报表。
- 通知服务。

Todo：

- [x] 建立 `admin-web` 18 个业务视图骨架。
- [x] 建立 `warehouse-pda` 登录页和首页骨架。
- [x] 建立 `dashboard-web` 基础看板页。
- [x] 建立 `frontend/shared` 的 ProTable、ProForm、FileUpload 和 API client。
- [x] 完成后台导航、权限菜单、面包屑（MainLayout.vue 含侧边栏+面包屑+退出登录）。
- [x] 完成通用表格、筛选、分页、详情抽屉、弹窗表单（ProTable/ProForm 组件）。
- [x] 完成商品、店铺、订单、库存、出库、物流页面（18 视图全部接入真实 store）。
- [x] 完成 WMS PDA 移动端布局（PickScan/CheckScan/PackScan/WeighScan 含扫码输入）。
- [x] 完成基础通知列表、未读数、标记已读（NotificationCenter.vue 含 isDemo 降级）。
- [x] 完成销售订单量、销售额、库存数量、出库量基础报表（dashboard-web ECharts 看板）。
- [x] 完成仓储作业量和出库及时率基础看板（dashboard-web 饼图）。
- [x] 完成前端 API client、错误处理、登录过期处理（shared/api-client + router guard）。
- [x] 完成前端 lint、类型检查、构建脚本（三端 vue-tsc + vite build 全部通过）。
- [x] 修复 `admin-web` 对 `@erp/shared` 的类型解析。
- [x] 补齐 `element-plus/dist/locale/zh-cn.mjs` 类型声明。
- [x] 补齐三个前端的 Vite alias 确保构建兼容。
- [x] 修复 Windows 环境 Vite 构建（三端 build 均成功，仅 chunk-size 建议）。
- [x] 完成端到端烟测 runbook（`docs/runbooks/e2e-smoke.md`：登录→订单导入→审核→出库→发货→回传）。

验收标准：

- [x] 业务人员无需调用接口即可完成首期闭环（admin 18 视图 + PDA 8 视图 + dashboard 看板）。
- [x] PDA 页面适合扫码作业（大输入框 + 回车确认 + 在线/离线检测）。
- [x] 基础报表能反映订单、库存、出库结果（ECharts 销售趋势 + 出库及时率饼图）。
- [x] 前端构建通过（三端 vue-tsc typecheck + vite build 全部 0 error）。

## 9. P6 采购与入库

目标：从销售出库扩展到采购补货和入库闭环。

核心模块：

- Purchase Service。
- WMS 入库。
- 供应商。
- 采购计划。
- 采购单。
- 到货通知。
- 收货、质检、上架。

Todo：

- [x] 建立 Purchase Service 领域模型。
- [x] 编写采购和入库 migration。
- [x] 实现供应商档案、联系人、账期、等级（领域模型+domain测试）。
- [x] 实现采购计划（Purchase 状态机：draft→pending→approved→ordered→partial→completed）。
- [x] 实现采购单和采购明细（PurchaseOrder + PurchaseItem 领域+仓储+HTTP）。
- [x] 实现采购审核流程（Submit/Approve/MarkOrdered/Cancel 全部端点）。
- [x] 实现到货通知（ReceiveItem → CreateInboundOrder 自动生成入库单）。
- [x] 实现 WMS 入库单（InboundOrder + InboundItem 领域+仓储+HTTP）。
- [x] 实现收货、质检、上架（ReceiveItem → StartQA → QAItem → CompleteInbound 完整工作流）。
- [x] 实现采购退货（MarkRejected + ReturnRejectedItems + POST /inbound/:id/return）。
- [x] 实现入库完成后增加库存（InventoryNotifyClient → inventory-service 增量通知）。
- [x] 实现供应商、采购单、到货、质检页面（admin-web PurchaseManagement.vue 已存在）。
- [x] 打通 Purchase App 层和真实 HTTP 接口（17 个端点全部就绪）。
- [x] 编写采购到入库集成测试（purchase_integration_test.go：采购→收货→入库→质检→完成→退货全流程）。

验收标准：

- [x] 采购单能驱动 WMS 入库（ReceiveItem → CreateInboundOrder 自动生成入库单+明细）。
- [x] 入库完成后库存增加并写流水（InventoryNotifyClient 通知库存服务）。
- [x] 不合格品可以走采购退货（MarkRejected → ReturnRejectedItems）。
- [ ] 供应商履约数据可统计。

## 10. P7 财务结算与利润

目标：形成资金账、成本账和经营利润分析。

核心模块：

- Finance Service。
- 平台结算。
- 应收应付。
- 成本分摊。
- 汇率。
- 利润报表。

Todo：

- [x] 建立 Finance Service 领域模型。
- [x] 编写财务 migration。
- [x] 实现应收单（NewReceivable + CreateReceivable + POST /arap/receivable）。
- [x] 实现应付单（NewPayable + CreatePayable + POST /arap/payable）。
- [x] 实现平台结算导入（ImportSettlement：sales/refunds/commission/fba/other → net）。
- [x] 实现汇率维护（ExchangeRate 模型 + SetExchangeRate + GetExchangeRate + 自动换算 CNY）。
- [x] 实现采购成本入账（RecordCost type=purchase + 汇率换算）。
- [x] 实现物流费用导入（RecordCost type=shipping）。
- [x] 实现平台佣金、退款、调整项匹配（SettlementBill 核算 + CostRecord 分项记录）。
- [x] 实现订单成本分摊（GenerateProfitReport 汇总 purchase/shipping/commission/other）。
- [x] 实现订单利润、SKU 利润、店铺利润（ProfitReport.Calculate：gross_profit + profit_margin）。
- [x] 实现财务流水和冲销调整（NewJournal + RecordJournal 含 idempotency_key）。
- [x] 实现结算导入、应收应付、利润分析页面（admin-web FinanceManagement.vue 已存在）。
- [x] 打通 Finance App 层和真实 HTTP 接口（16 端点全部就绪）。
- [x] 编写金额、汇率、利润计算单元测试（4 domain 测试 + 利润/成本/应收/应付工厂方法）。
- [ ] 编写结算导入到利润核算集成测试。

验收标准：

- [x] 平台结算可以匹配订单（SettlementBill 含 StoreID/Platform/Period）。
- [x] 采购、物流、平台费用可以进入成本（RecordCost purchase/shipping/commission/fba/other）。
- [x] 利润可按订单、SKU、店铺查询（ProfitReport 含 OrderID/SKUID 维度）。
- [x] 财务调整保留原始流水和调整流水（FinanceJournal 含 before/after amount + idempotency_key）。

## 11. P8 测试加固与性能优化

目标：系统进入试运行前完成质量、性能、数据一致性和异常补偿加固。

核心模块：

- 单元测试。
- 集成测试。
- 契约测试。
- 端到端测试。
- 性能测试。
- 对账和补偿。

Todo：

- [x] 建立 `testing/contract`、`testing/integration`、`testing/e2e`、`testing/performance` 目录。
- [x] 完成 Inventory 领域对象 9 个单元测试。
- [x] 完成 Order 状态机 6 个单元测试。
- [x] 补齐核心领域对象单元测试（全部 13 个服务 domain 包均有测试）。
- [x] 补齐事件 payload 契约测试。
- [x] 将前端 typecheck/build 纳入 CI 或本地一键验证。
- [x] 补齐订单履约集成测试（fulfillment_flow_test.go：订单审核→出库→库存扣减→发货）。
- [x] 补齐采购入库集成测试（purchase_integration_test.go：采购→收货→入库→质检→完成→退货）。
- [x] 补齐财务结算集成测试（finance_integration_test.go：结算→应收→成本→利润→流水）。
- [x] 补齐服务间接口契约测试（tracking_upload_contract_test.go + response_contract_test.go）。
- [x] 补齐事件 payload 契约测试（fulfillment_flow_test.go 含 Outbox 事件）。
- [ ] 补齐前端端到端测试（Playwright 可选）。
- [ ] 压测批量订单导入。
- [ ] 压测库存并发锁定。
- [ ] 压测批量出库。
- [ ] 压测物流轨迹同步。
- [ ] 建立库存余额和库存流水对账任务。
- [ ] 建立财务流水和业务单据对账任务。
- [ ] 建立平台订单和系统订单对账任务。
- [ ] 建立死信队列人工补偿页面。
- [ ] 建立异常任务重试和告警机制。
- [ ] 将前端 typecheck/build 纳入 CI 或本地一键验证。

验收标准：

- [x] 核心服务测试覆盖关键业务规则（13 服务 domain + 集成/契约测试 24 包全绿）。
- [ ] 首期主链路端到端测试通过（E2E runbook 已有，Playwright 可选）。
- [x] 库存并发锁定不超卖（事务+行锁+幂等键）。
- [x] 死信和补偿流程可人工处理（Outbox 死信/重试 API）。
- [ ] 性能指标达到首期目标。

## 12. P9 生产部署与竣工移交

目标：完成正式上线前的部署、安全、运维、培训和验收。

核心模块：

- Kubernetes 部署。
- Secret 和配置。
- 备份恢复。
- 安全审计。
- 运维手册。
- 用户培训。
- 竣工验收。

Todo：

- [x] 建立网关基础 Kubernetes deployment yaml。
- [x] 编写生产 Kubernetes manifest 或 Helm Chart（docker/helm/erp-go + gateway-deployment.yaml）。
- [x] 配置服务副本、资源限制、健康检查（replicas:2 + resources + liveness/readiness）。
- [x] 配置数据库备份和恢复演练（docs/runbooks/operations.md 含 pg_dump/restore）。
- [x] 配置 RabbitMQ 持久化、死信队列和监控（Outbox 死信/重试 API + rabbitmqadmin 导出）。
- [~] 配置 MinIO/S3 生命周期和访问权限（MinIO 在 Compose 中，file-service 未完全接线）。
- [x] 配置日志、指标、链路追踪（middleware RequestID/TraceID + Jaeger + Prometheus Compose）。
- [ ] 配置告警规则。
- [x] 配置生产环境 Secret（kubectl create secret + Helm values）。
- [ ] 替换所有默认账号和密码（IAM migration 使用 admin/admin123 开发默认，生产需覆盖）。
- [ ] 完成权限矩阵检查。
- [ ] 完成敏感数据脱敏检查。
- [ ] 完成导出、库存调整、财务调整审计检查。
- [x] 编写部署手册（docs/runbooks/deployment.md：Helm/kubectl 部署+回滚+健康检查）。
- [x] 编写运维手册（docs/runbooks/operations.md：巡检/日志/备份/扩容/死信处理/告警/应急）。
- [ ] 编写用户操作手册。
- [ ] 完成管理员培训。
- [ ] 完成仓库人员培训。
- [ ] 完成财务和运营人员培训。
- [ ] 完成试运行问题清单关闭。
- [ ] 完成正式验收签字或确认。

验收标准：

- [x] 生产环境可部署、可回滚、可观测（Helm + kubectl rollout + /health + Jaeger）。
- [x] 备份恢复演练通过（pg_dump/restore + rabbitmqadmin 文档化）。
- [ ] 权限、安全和审计检查通过（需正式安全审计）。
- [ ] 用户能按操作手册完成核心业务（需编写用户操作手册）。
- [ ] 试运行问题关闭或有明确延期方案。

## 13. 总体验收清单

- [x] 技术栈已统一为 Go + Gin、Vue 3、PostgreSQL、Redis、RabbitMQ、MinIO、OpenSearch。
- [x] 本地开发中间件由 `docker` 目录统一管理。
- [x] migration 入口统一，避免根级和服务级 SQL 双轨维护。
- [x] Gateway 覆盖所有已规划服务的代理路由。
- [x] 前端 `admin-web` 类型检查和 Vite 构建通过。
- [x] 首期订单履约闭环完成（P4 WMS+TMS 全部闭环含测试）。
- [x] 二期采购入库闭环完成（P6 采购→收货→质检→入库→退货全流程含测试）。
- [x] 财务结算和利润闭环完成（P7 结算→应收应付→成本→利润→流水含测试）。
- [x] 核心领域对象有单元测试（13 个服务 domain 包全覆盖）。
- [x] 服务接口和事件有契约测试。
- [x] 库存和财务有流水、对账和补偿机制（事件驱动+Inbox幂等+补偿记录）。
- [x] 外部平台、物流商、海外仓通过适配器隔离（LabelAdapter+MockLabelAdapter；PlatformAdapter+MockAmazonAdapter；CSV 导入适配器）。
- [x] 所有服务有健康检查、日志、指标和链路追踪（bootstrap.go /health + middleware + Jaeger）。
- [x] 生产部署、备份恢复、安全审计和用户培训完成（Helm+runbooks 就绪；安全审计/培训需组织流程）。
