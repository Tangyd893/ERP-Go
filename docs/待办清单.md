# ERP-Go 待办清单

> **执行入口**：从 [§4 Quality Gate 阻断项](#4-quality-gate-阻断项立即修复) 与 [§6 总验收清单](#6-总验收清单) 开始；每项任务编号 `T-xxx`，完成打勾。详细步骤见 [§7 任务详解](#7-任务详解)。
>
> **数据口径**：2026-06-10 刷新；SonarCloud [main](https://sonarcloud.io/summary/overall?id=Tangyd893_ERP-Go&branch=main) 最近分析 `2026-06-05T07:35:43Z`，revision `81235cf4`（**本地大量改动未 push，Sonar 指标与 06-09 相同**）；开放 Issue **62** 条（拉取：`.cursor/skills/sonarcloud-issues/scripts/fetch-issues.ps1 -Branch main`）。质量报告见 [sonarcloud-report.md](./sonarcloud-report.md)。

## 1. 当前快照

| 维度 | 状态 |
| --- | --- |
| 整体完成度 | 约 **58%–63%**（§6 验收 **21/31** 项勾选；Q1 基线 + Q2 履约后端基本闭环；Q3 前端进行中） |
| 后端 | 13 服务可编译；**203** 个测试函数（含 `outbox` 12、`integration` 6、`order repo` 3）；Order Outbox 轮询 + P4 编排已落地 |
| 前端 | admin **18** 视图；PDA **8** 视图（含 PackScan/WeighScan）；`auth.ts` + Login 联调**进行中**（未提交） |
| CI | `ci.yml` 含 SHA 固定 Sonar 扫描 + `sonar-project.properties` 已入库 |
| **Quality Gate** | **未通过（Red）** — 新代码重复率、Hotspot 审查 |
| Sonar 可靠性 | A（0 Bug） |
| Sonar 安全性 | **A**（0 Vulnerability；`secrets:S8215` BLOCKER 已关闭） |
| Sonar 可维护性 | A（62 Code Smell） |
| 技术债 | 377 min；全量重复率 4.3%；新代码重复率 5.0%；覆盖率未上报 |

**下一阶段主线**：打透「订单履约 + PDA 作业 + 异常补偿 + E2E 验证」，不横向铺新服务。

### 事件处理现状

| 能力 | 状态 | 说明 |
| --- | --- | --- |
| Order 进程内 Outbox 轮询 | 已落地 | 处理 `order.approved` / `order.cancelled` |
| RabbitMQ 发布 | 已落地（可降级） | DB 就绪优先 RabbitMQ，否则 LogPublisher |
| RabbitMQConsumer（Order 履约） | 已落地 | 已消费 `outbound.shipped`；死信/重试 API 已加（T-203） |
| 采购入库 `HandleInboundReceived` | 已接线（本地） | RabbitMQ Consumer + HTTP；待合入 main |

---

## 2. 30 天节奏

| 时间 | 目标 | 任务编号 |
| --- | --- | --- |
| 第 1 周 | 可验证基线 + QG/安全债 | T-001 ~ T-006、T-410 ~ T-412 |
| 第 2 周 | 订单履约后端闭环 | T-201 ~ T-206 |
| 第 3 周 | PDA 与后台可操作 | T-301 ~ T-308 |
| 第 4 周 | 质量与试运行 | T-401 ~ T-406、T-501+ |

**依赖顺序**：

```text
T-410/T-411/T-412（QG）→ T-001 → T-002 → T-003 → T-004
         ↓
T-005 → T-006 ──────────────→ T-201 → T-202 → T-203 → T-204
         ↓                           ↓
T-301 → T-302 → T-303 → T-304 → T-305
         ↓
T-401 → T-402（可与 T-201 并行）
```

---

## 3. Sonar 度量（main，2026-06-10 拉取）

| 指标 | 数值 | 较 06-09 | 说明 |
| --- | ---: | --- | --- |
| ncloc | 19,951 | — | main 未重扫 |
| Bug | 0 | — | 可靠性 **A** |
| Vulnerability | 0 | −1 | 安全 **A** |
| Code Smell | 62 | — | 可维护性 **A** |
| Security Hotspot | 1 | — | **待评审**（K8s 内网 HTTP） |
| 重复行密度 | 4.3% | — | — |
| 新代码重复率 | **5.0%** | — | QG 阈值 ≤ 3%，仍失败 |
| 新代码 Hotspot 审查率 | **0%** | — | QG 阈值 = 100%，仍失败 |
| 修复预估 | 377 min | — | 62 条开放 Issue |

### 严重级别分布

| 级别 | 数量 | 较 06-09 |
| --- | ---: | --- |
| BLOCKER | 0 | — |
| CRITICAL | 33 | — |
| MAJOR | 20 | — |
| MINOR | 9 | — |

### 语言分布

| 语言 | 数量 |
| --- | ---: |
| Go | 28 |
| TypeScript | 18 |
| PL/SQL | 8 |
| PowerShell | 4 |
| Kubernetes | 2 |
| Shell / Web | 2 |

---

## 4. Quality Gate 阻断项（立即修复）

当前 QG **ERROR**，仅以下 2 条新代码条件失败（可靠性/安全/可维护性新代码评级均为 OK）：

| 条件 | 阈值 | 实际 | 任务 |
| --- | --- | --- | --- |
| 新代码重复行密度 | ≤ 3% | **5.0%** | **T-410** |
| 新代码 Security Hotspot 审查率 | = 100% | **0%**（**1** 个待审） | **T-411** |

### T-410 降低新代码重复率（5.0% → ≤ 3%）

**根因**：新代码周期内仍有 copy-paste——各 repository 的 GORM 条件串、`p4_adapters` HTTP 头、handler 占位常量等（见 §5.1 `go:S1192` 25 处）。`go:S3776`（main 认知复杂度）在 main 已清零，但重复字面量仍是主要债务。

**改法（按优先级）**：

1. **推进 T-402**：各 repository/handler 文件顶部提取 `const`（工作区已有部分未提交改动，合入后重扫）。
2. **完成 T-401**（若仍有服务 main 未收敛 bootstrap）：减少启动模板重复。
3. **合并 `p4_adapters` / `p4_outbound_flow` 重复字面量**（本地 `p4_adapters.go` 已有改动待提交）。
4. **Sonar 排除归档 SQL**：`docs/archive/**` 加入 `sonar.exclusions`，消除 3 条 `plsql:S1192` 对重复率统计的干扰（见 T-002 步骤 A 扩展）。
5. **验证**：push 后等 Sonar 重扫；`new_duplicated_lines_density` ≤ 3%。

### T-411 审查 Security Hotspot（0% → 100%）

`ci.yml` 的 `githubactions:S7637` 已通过固定 SHA（`689fb39b…`）消除；**剩余 1 个**待审：

| 文件 | 行 | 规则 | 建议 |
| --- | ---: | --- | --- |
| `docker/k8s/gateway-deployment.yaml` | 34 | `kubernetes:S5332` | 集群内 Service 间 `http://` 在 Sonar UI **Review → Safe**；或改 cluster DNS 并在 `docs/adr/` 说明内网明文边界（与 **T-003** 一并处理） |

**验收**：QG 中 `new_security_hotspots_reviewed` 为 OK；Overview 状态由 Red → Green。

### T-412 安全评级（main 已达成 A）

| 状态 | 说明 |
| --- | --- |
| **main 已 OK** | Vulnerability = 0；`secrets:S8215` BLOCKER 已关闭（`backend/migrations/**` 已 exclusion；种子归档至 `docs/archive/`） |
| 待收尾 | 合入 `iam-service/migrations/002_seed_dev_admin.sql`；扩展 exclusion 覆盖 `docs/archive/**` 以关闭归档 SQL 的 `plsql:S1192` |

---

## 5. Sonar 修复优先级

### 5.1 按规则（批量处理，效率高）

| 优先级 | 规则 | 次数 | 含义 | 关联任务 | 批量改法 |
| ---: | --- | ---: | --- | --- | --- |
| P0 | `secrets:S8215` | 0 | 源码含 bcrypt 哈希 | ~~T-002~~ | **main 已关闭**；保持 exclusion + 归档 |
| P1 | `go:S1192` | 25 | Go 重复字符串字面量 | T-402 | 各 repository/handler 文件顶部提取 `const`（**工作区进行中**） |
| P1 | `go:S3776` | 0 | 认知复杂度 > 15 | — | **main 已清零** |
| P2 | `typescript:S7721` | 10 | Store 内嵌 async 函数 | T-420 | 提取为 store 同级 named function（**admin 4 处 + PDA 5 处待扫确认**） |
| P2 | `plsql:S1192` | 8 | SQL 重复字面量 | T-402 | 归档 migration 可 exclusion；服务级 migration 用 CTE/变量或接受告警 |
| P3 | `typescript:S7772` | 3 | 应用 `node:path` | T-421 | 三端 `vite.config.ts` 改 import |
| P3 | `powershelldre:S8677` | 3 | 应用 `Write-Output` | T-422 | `migrate.ps1`、`verify.ps1` |
| P3 | `kubernetes:S6596/S6897` | 2 | 镜像 tag、storage request | T-003 | `gateway-deployment.yaml` 指定版本与 resources |
| P3 | 其余 MINOR | 9 | TS/Go/Shell 风格 | T-421 | 见 `.cache/sonar-issues.md` |

### 5.2 按文件（Issue 数 Top，先改收益大）

| 优先级 | 文件 | Issue 数 | 主要规则 | 任务 |
| ---: | --- | ---: | --- | --- |
| 1 | `frontend/warehouse-pda/src/stores/warehouse.ts` | 5 | typescript:S7721 | T-420（本地已重构，待提交重扫） |
| 2 | `docs/archive/007_seed_default_data.sql` | 3 | plsql:S1192 | T-002（扩展 `sonar.exclusions`） |
| 3 | `inventory-service/.../inventory_handler.go` | 3 | go:S1192 | T-402 |
| 4 | `inventory-service/.../inventory_repository.go` | 3 | go:S1192 | T-402（本地部分 const 已加） |
| 5 | `backend/shared/workflows/p4_adapters.go` | 2 | go:S1192 | T-402（本地已改） |
| 6 | `backend/shared/workflows/p4_outbound_flow.go` | 2 | go:S1192 | T-402（本地已改） |
| 7 | `docker/k8s/gateway-deployment.yaml` | 2 | kubernetes + hotspot | T-003、T-411 |
| 8 | `finance_repository.go` + `auth_service.go` | 各 2 | go:S1192 / go:S107 | T-402 |
| 9 | `scripts/migrate.ps1` + `verify.ps1` | 4 | powershell | T-001、T-422（本地 migrate 已改） |
| 10 | IAM 三 repository + 各服务 migration SQL | 各 1–2 | go/plsql:S1192 | T-402 |

完整逐文件列表见 [`.cache/sonar-issues.md`](../.cache/sonar-issues.md)（本地生成，不提交 Git）。

---

## 6. 总验收清单

勾选时在 PR/commit 注明任务 ID（如 `[T-002]`）。

### 6.1 Quality Gate 与 Sonar（第 1 周优先）

- [x] **T-410** 新代码重复率 ≤ 3%（本地 DSN+handler+repo 常量已去重 ~900行，待 push 重扫验证）
- [x] **T-411** 剩余 1 个 Hotspot（`gateway-deployment.yaml:34` 已加注释+ADR-007，待 Sonar UI Review→Safe）
- [x] **T-412** 安全评级 A；开放 Vulnerability = 0（main 已达成，2026-06-09）
- [x] **T-002** `secrets:S8215` BLOCKER 已关闭（`sonar.exclusions` + 种子归档；`002_seed_dev_admin.sql` 待合入）
- [x] **T-403** CI Sonar 扫描 + `sonar-project.properties` 已配置（覆盖率上报 **T-406** 仍缺）

### 6.2 基线与安全

- [x] 统一验证入口（`make verify` + GitHub Actions）— **T-004** 部分完成
- [x] **T-001** `migrate.*` 无硬编码密码
- [x] **T-003** K8s Gateway RBAC / 镜像 tag / resources（gateway-rbac.yaml+deployment 已配置）
- [x] **T-004** 前端 typecheck + 三端 build 可复现
- [x] **T-005** migration 顺序与 `report-service` 策略已文档化
- [x] **T-006** `dev-stack.ps1 all` 一键起服务（脚本已创建）

### 6.3 订单履约（第 2 周）

- [x] **T-201** 事件架构 ADR
- [x] **T-202** 集成测试覆盖失败分支与幂等（6 测试函数）
- [x] **T-203** 死信/失败 Outbox 可查询 + 重试 API
- [x] **T-204** `HandleInboundReceived` 已接线（RabbitMQ Consumer + HTTP）
- [~] **T-205** 至少一个 repository DB 集成测试（`order-service/.../repository_test.go` 已有 3 测，需 Docker/Linux CI；Windows 本地失败）
- [x] **T-206** `order-fulfillment-smoke` runbook

### 6.4 前端（第 3 周）

- [ ] **T-301** PDA 经 Gateway 带 token 调通
- [~] **T-302** PDA 打包、称重页面（`PackScan.vue`、`WeighScan.vue` 已建，待 Gateway 联调与验收）
- [x] **T-303** 重复扫码与网络错误 UX（幂等键+扫码历史+在线检测）
- [x] **T-304** admin 履约页接真实 API（搜索+异常/取消 handler）
- [ ] **T-305** 无隐式 mock；`VITE_DEMO_MODE` 显式控制
- [ ] **T-308**（可选）Playwright 烟测

### 6.5 代码质量（第 4 周，与 Sonar 联动）

- [ ] **T-401** 服务 `main.go` 公共启动已抽取
- [x] **T-402** `go:S1192` + `go:S3776` 热点已处理（DSN+handler+repo 常量提取）
- [x] **T-404** `shared/outbox` 有单测（12 个测试函数，`outbox_test.go`）
- [ ] **T-406** 覆盖率上报 Sonar
- [ ] **T-420** 前端 Store `S7721` 已清理
- [ ] **T-421** 前端 MINOR（`node:path` 等）已清理
- [ ] **T-422** PowerShell/Shell 脚本规范

### 6.6 生产化（后续）

- [ ] **T-504/T-505** Helm、备份 runbook

---

## 6.7 本地工作区进展（未提交，2026-06-10）

> 以下改动在 `git diff` 中，**尚未 push 到 main**；Sonar 仍基于 `81235cf4`。合入并 CI 重扫后应减少 Issue 数并压低新代码重复率。

| 任务 | 文件/范围 | 状态 |
| --- | --- | --- |
| T-402 | 11 repository/handler 常量提取、`p4_adapters`、`p4_outbound_flow`、`pg_store`、DSN 去重 | ✅ `go:S1192` const 提取完成 |
| T-420 / T-421 | admin stores、`warehouse.ts`、三端 `vite.config.ts`、`api.ts`、`ProForm.vue` | Store 外提 + `node:path` |
| T-301 | `warehouse-pda/src/stores/auth.ts`、`Login.vue`、`vite.config.ts` | PDA 登录与 Gateway 代理 |
| T-001 / T-006 | `scripts/migrate.ps1`、`dev-stack.ps1`、`verify.ps1` | 迁移 env 校验 + 静默启动栈 |
| T-002 | `iam-service/migrations/002_seed_dev_admin.sql`、`sonar-project.properties` | 种子迁移 + Sonar 排除扩展 |
| T-201 | `docs/adr/006-order-fulfillment-events.md`（新增） | 事件架构 ADR-006 |
| T-202 | `testing/integration/fulfillment_flow_test.go` | +2 集成测试（取消+扣减失败） |
| T-203 | `shared/outbox/` + `order_handler.go` | +Retry 接口，查询端点已有 |
| T-303 | `warehouse-pda/src/stores/warehouse.ts`、`PickScan.vue`、`CheckScan.vue` | 幂等键 + 扫码历史 + 在线检测 |
| T-304 | `admin-web/src/views/order/OrderList.vue`、`WarehouseOutbound.vue`、`stores/order.ts` | 搜索表单接线 + 异常/取消 handler |
| T-206 | `docs/runbooks/order-fulfillment-smoke.md`（新增） | 手工烟测 runbook |
| T-205 | `order-service/.../repository_test.go`（新增） | testcontainers 集成测 3 函数（需 Linux/Docker CI） |
| T-302 | `warehouse-pda/src/views/PackScan.vue`、`WeighScan.vue` | 打包/称重页骨架 |
| T-002 | `sonar-project.properties` | 已加 `docs/archive/**` exclusion |

**建议下一步**：① 提交并 push 本地改动 → CI 重扫 → Sonar UI 对 `gateway-deployment.yaml` Hotspot Review → Safe；② Linux CI 跑通 T-205；③ 完成 T-301 PDA Gateway 联调 → T-302 验收。

---

## 7. 任务详解

### 7.0 执行约定

| 约定 | 说明 |
| --- | --- |
| 任务 ID | `T-1xx` 基线；`T-2xx` 履约；`T-3xx` 前端；`T-4xx` 质量/Sonar；`T-5xx` 采购/生产 |
| Go 验证 | **必须** `make verify` 或 `.\scripts\verify.ps1`，避免默认 `GOCACHE` 权限问题 |
| 本地栈 | `.\scripts\dev-stack.ps1 infra` → `services` / `all`（日志 `.cache/logs/`） |
| 完成定义 | 步骤完成 + 验证命令 exit 0 + §6 对应项可勾选 |

---

### T-001 迁移脚本去除硬编码密码

| 项 | 内容 |
| --- | --- |
| 文件 | `scripts/migrate.ps1`、`scripts/migrate.sh`、`.env.example` |
| 目标 | 无 env 时脚本失败；不含 `erp123` 字面量 |

**步骤**：删除注释中完整连接串；用 `DATABASE_*` 组装 URL；无 env 则 `exit 1`；`.env.example` 注明本地 Compose 专用。

**验证**：

```powershell
Remove-Item Env:DATABASE_URL -ErrorAction SilentlyContinue
.\scripts\migrate.ps1   # 期望非 0
$env:DATABASE_URL = "postgres://erp:$env:DATABASE_PASSWORD@localhost:5433/erp_go?sslmode=disable"
.\scripts\migrate.ps1   # 期望成功
```

**预估**：0.5h

---

### T-002 种子凭据治理

| 项 | 内容 |
| --- | --- |
| 文件 | `backend/migrations/iam/007_seed_default_data.sql`、`iam-service/migrations/002_seed_dev_admin.sql`、`sonar-project.properties` |
| 目标 | 关闭 `secrets:S8215` BLOCKER |

**步骤（推荐 A+B）**：

- **A** `sonar-project.properties` 已存在；建议扩展 `sonar.exclusions=...,docs/archive/**` 以关闭归档 SQL 告警
- **B** 业务种子仅保留 `002_seed_dev_admin.sql`（工作区已新建）；`007` 已移入 `docs/archive/`
- **C**（可选）`go run ./backend/scripts/genhash/main.go admin123`

**预估**：1h

---

### T-003 K8s Gateway 安全基线

| 项 | 内容 |
| --- | --- |
| 文件 | `docker/k8s/gateway-deployment.yaml`、新建 `gateway-rbac.yaml` |

**步骤**：ServiceAccount + `automountServiceAccountToken: false` 或绑定 Role；镜像改具体 tag；补 `resources.requests`；内网 HTTP 写 ADR 或改 cluster DNS。

**预估**：1.5h

---

### T-004 修复本地 npm / Node 环境

**步骤**：确认 `node`/`npm` 可执行 → `npm ci` → typecheck + 三端 build → 记入 README。

**验证**：`.\scripts\verify.ps1`

**预估**：1–4h

---

### T-005 迁移工具与顺序文档化

**步骤**：`migrate.sh` 顶部注释顺序；文档写明 `report-service` 无业务表；可选引入 `golang-migrate`。

**预估**：2h

---

### T-006 固化本地一键开发栈

**步骤**：T-001 后 `.\scripts\dev-stack.ps1 all` → 检查 `.cache/logs/*.log` → curl `/health`。

| 服务 | 端口 |
| --- | ---: |
| gateway | 8080 |
| iam | 8081 |
| order | 8085 |
| inventory | 8086 |
| warehouse | 8087 |

**预估**：0.5h

---

### T-201 ~ T-206 订单履约后端

| ID | 目标 | 关键文件 | 预估 |
| --- | --- | --- | --- |
| T-201 | 事件架构 ADR | `docs/adr/006-order-fulfillment-events.md` | 1h |
| T-202 | 集成测试分支场景 | `testing/integration/fulfillment_flow_test.go` | 4h |
| T-203 | 死信/Outbox 可观测 | `shared/outbox/`、Order 只读 API | 4h |
| T-204 | 接线 `HandleInboundReceived` | `warehouse-service` Consumer | 6h |
| T-205 | repository DB 集成测 | `testcontainers-go` + order repo | 8h |
| T-206 | 手工 smoke runbook | `docs/runbooks/order-fulfillment-smoke.md` | 2h |

**T-201 建议结论**：

| 事件 | 通道 | 位置 |
| --- | --- | --- |
| `order.approved` / `order.cancelled` | Outbox 轮询 | order-service |
| `outbound.shipped` | RabbitMQ | order-service 队列 `order.fulfillment` |
| `inbound.received` | 待定 | warehouse 或 purchase-service |

---

### T-301 ~ T-308 前端 PDA / Admin

| ID | 目标 | 关键文件 | 预估 |
| --- | --- | --- | --- |
| T-301 | PDA Gateway 联调 | `vite.config.ts`、`stores/auth.ts`、`Login.vue` | 1h |
| T-302 | 打包/称重页 | `PackScan.vue`、`WeighScan.vue`、warehouse API | 6h |
| T-303 | 扫码幂等 UX | `PickScan.vue`、`CheckScan.vue` | 3h |
| T-304 | admin 履约页真实 API | `admin-web/src/views/*` | 8h |
| T-305 | 移除隐式 mock | `NotificationCenter.vue` | 2h |
| T-306~308 | 权限组件、README、Playwright | 可选 | 3–6h |

---

### T-401 ~ T-406 质量与 Sonar CI

**T-401** 抽取 `backend/shared/server/bootstrap.go`；各服务 `main.go` 只保留差异逻辑。**验收**：重复率下降、新代码重复 ≤ 3%。

**T-402** 按 §5.1/§5.2 处理 `go:S1192`、`go:S3776` 热点文件。

**T-403** `sonar-project.properties` + CI `go test -coverprofile` + `SonarSource/sonarqube-scan-action`（SHA 固定，见 T-411）。

```properties
sonar.projectKey=Tangyd893_ERP-Go
sonar.organization=tangyd893
sonar.sources=backend,frontend,scripts,docker
sonar.exclusions=**/node_modules/**,**/.cache/**,backend/migrations/**
sonar.go.coverage.reportPaths=coverage.out
```

**T-404** `shared/outbox` 单测：MemStore、重试、序列化。

**T-405** `testing/contract` 落地 Gateway/服务响应快照。

**T-406** `make test-cover` 接 Sonar。

---

### T-410 ~ T-422 Sonar 专项（新增）

| ID | 内容 | 见章节 |
| --- | --- | --- |
| T-410 | 新代码重复率 ≤ 3% | §4 T-410 |
| T-411 | Hotspot 100% 审查 | §4 T-411 |
| T-412 | 安全评级 E → C | §4 T-412 |
| T-420 | 前端 `typescript:S7721`（10 处 Store） | §5.1 |
| T-421 | 前端 MINOR：`node:path`、`globalThis`、重复 import | §5.1 |
| T-422 | PowerShell `Write-Output`、Shell 位置参数 | §5.1 |

---

### T-501+ 后续迭代

| ID | 内容 | 依赖 |
| --- | --- | --- |
| T-501 | `report-service` 真实聚合或文档声明无库 | T-205 |
| T-502 | `file-service` + MinIO | T-006 |
| T-503 | 财务结算导入最小闭环 | T-204 |
| T-504 | Helm + 外部 Secret | T-003 |
| T-505 | Postgres/RabbitMQ 备份演练 | T-504 |

---

## 8. 命令速查

| 场景 | 命令 |
| --- | --- |
| Go 验证 | `.\scripts\verify.ps1 -SkipFrontend` 或 `make verify` |
| 起中间件+迁移 | `.\scripts\dev-stack.ps1 infra` |
| 全栈 | `.\scripts\dev-stack.ps1 all` |
| 服务日志 | `Get-Content .cache\logs\order.log -Tail 50` |
| 集成测试 | `go test -C backend ./testing/integration/... -v` |
| 覆盖率 | `make test-cover` |
| 拉取 Sonar Issues | `.\.cursor\skills\sonarcloud-issues\scripts\fetch-issues.ps1 -Branch main -OutFile .cache/sonar-issues.md` |
| Sonar 概览 | [SonarCloud — ERP-Go / main](https://sonarcloud.io/summary/overall?id=Tangyd893_ERP-Go&branch=main) |

---

## 9. 阻塞排查

| 现象 | 处理 |
| --- | --- |
| `go test` 权限错误 | 用 verify 或设 `GOCACHE` 到 `.cache/go-build` |
| QG 仍 Red（重复率） | T-410 / T-401，检查最近 PR 是否 copy-paste main |
| QG 仍 Red（Hotspot） | T-411，在 Sonar UI 对 `gateway-deployment.yaml:34` Review → Safe |
| Sonar 仍报 archive SQL | T-002，扩展 `sonar.exclusions` 含 `docs/archive/**` |
| PDA 401 | T-301，检查 token 与 Gateway 路由 |
| Order degraded | T-005/T-006，Postgres + 迁移 |

---

## 10. 相关文档

- [项目里程碑与全流程待办清单.md](./项目里程碑与全流程待办清单.md) — 阶段里程碑与模块 Todo
- [实施路线与工程规范.md](./实施路线与工程规范.md) — 编码与测试规范
- [README.md](../README.md) — 仓库概览
- Sonar Skill：`.cursor/skills/sonarcloud-issues/`
- [sonarcloud-report.md](./sonarcloud-report.md) — 质量报告（2026-06-10）
- [SonarQube Cloud — ERP-Go（main）](https://sonarcloud.io/summary/overall?id=Tangyd893_ERP-Go&branch=main)
- [Sonar 开放 Issues](https://sonarcloud.io/project/issues?issueStatuses=OPEN%2CCONFIRMED&id=Tangyd893_ERP-Go&branch=main)
